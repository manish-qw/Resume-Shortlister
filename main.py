"""
FastAPI application — API routes for resume evaluation.
Provides REST endpoints for the scoring engine.
"""

# TODO: Implement in Phase 8
# - POST /evaluate — PDF upload + JD text → EvaluationResponse
# - GET /results/{job_id} — retrieve cached results
# - GET /health — health check
# - In-memory cache: dict[str, EvaluationResult]
